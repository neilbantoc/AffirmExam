package neilbantoc.framework.view

/**
 * For view events/user actions that can't be bound
 * to layout through data binding.
 *
 * Example: Pull to refresh, viewpager page selected, etc.
 */
interface ObservableViewActions